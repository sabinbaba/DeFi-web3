global:
  scrape_interval: 5s # How often to grab data from your smart contract
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'smart_contract_monitor'
    static_configs:
      - targets: ['host.docker.internal:9991'] # This points to your Hardhat script running on Windows